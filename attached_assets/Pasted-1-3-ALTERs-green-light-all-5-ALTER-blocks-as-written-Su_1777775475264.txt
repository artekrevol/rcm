1. §3 ALTERs — green-light all 5 ALTER blocks as written. Subject to one quick check on my side before final go: the ALTERs presumably add the 10 missing columns as nullable (or with defaults) so the existing 2 rows don't fail the migration. If any ALTER adds a NOT NULL without a DEFAULT, that's the one to flag back. Otherwise, proceed as written. Don't rename anything in this sprint — name harmonization is a sprint 2 concern.
2. Demo rows — option (a), leave at pending. Two reasons. First, "demo data should remain demo data" is the right default; an active enrollment_status implies an operational state these rows don't represent. Second, I want to avoid mixing data hygiene with schema migration in the same sprint — keep the ALTERs purely structural, no UPDATEs. If the demo rows need cleanup, that's a separate, named decision in a later sprint, not a quiet UPDATE buried in a migration.
3. Drizzle drift on practicePayerEnrollments — add it to shared/schema.ts now. It's small, it's scoped, it's the right place for the definition to live, and the helper service layer in Step 6 needs typed access. Deferring it to sprint 1 means Step 6 helpers either bypass Drizzle for raw SQL (worse) or use loose types (also worse). Add the Drizzle definition now, matching the actual post-ALTER schema. Cheap to do in sprint 0, expensive to retrofit later.
4. Pool size — leave at default 10. No need to bump preemptively. Three orgs, dev DB, low concurrent load. If sprint 1 surfaces pool exhaustion when the helper layer goes live, bump it then with real evidence. Premature scaling is its own bug.
The audit-drift finding deserves explicit acknowledgment.
The fact that organizations columns are id, name, created_at, onboarding_dismissed_at, contact_email, status, updated_at — without slug or is_active — and that practice_settings.billing_model does exist when the audit said it didn't, means the audit itself has gaps. That's important context. The audit was honest about UNVERIFIED items, but these were claimed as VERIFIED and turned out wrong.
This doesn't invalidate the audit — it's still 90%+ accurate based on what we've checked — but it does change how I should treat audit claims going forward. "The audit said X" is no longer sufficient evidence to make architectural commitments. We should verify against live introspection on anything load-bearing.
Two specific consequences for sprint 0 and beyond:

The is_active field on profiles and the join table. v3 of the prompt has is_active BOOLEAN NOT NULL DEFAULT true on practice_profiles and similar fields elsewhere. That's fine for new tables. But if any seed or query in sprint 1 assumes organizations.is_active, it will break — the column doesn't exist; it's organizations.status with values like 'active'. Note this in the migration-state doc as a sprint 1 watch-out.
practice_settings.billing_model exists. This means there IS a column in the current schema doing the agency-billed branching that the architecture doc described. v3's profile JSONB edi_structural_rules.rendering_provider_loop_2310B.omit_when: "agency_billed" was written assuming this field doesn't exist and the logic lives somewhere else. Sprint 2 (when the EDI generator gets refactored to read from profile) needs to find every reference to practice_settings.billing_model and replace with profile-driven logic. Add this to the migration-state doc as a sprint 2 watch-out.

Neither of these blocks sprint 0. They're notes for the future, surfaced now while we have your attention on it.
One additional small ask before you resume:
Add a one-line entry to docs/architecture/sprint0-existing-schemas.md under a new "Audit drift discovered during Step 1" section listing the three audit gaps you found (no slug, no is_active, billing_model does exist). Future sprints will read these schema docs; they need to know the audit has corrections.
Proceed with Steps 2–9.
Stop after Step 5 (RLS enabled, middleware wired) for one more sign-off if anything unexpected surfaces — middleware wiring into server/index.ts is the riskiest single change in the sprint. If middleware insertion completes cleanly and the verification script in Step 5f passes, continue through Steps 6–9 without further pause. Final audit report when done.